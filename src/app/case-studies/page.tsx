import Link from "next/link";
import { Footer } from "@/components/Footer"; import { Header } from "@/components/Header"; import { caseStudies } from "@/content/site";
export const metadata = { title: "Case Studies" };
export default function CaseStudiesPage() { return <><Header /><main><section className="page-hero"><div className="container"><p className="eyebrow">Case studies</p><h1>Cloud outcomes that start with the right questions.</h1><p>Examples of the challenges Cloudvation is built to help teams solve. We’ll add approved customer stories as they become available.</p></div></section><section className="section"><div className="container"><div className="case-grid">{caseStudies.map((study, index) => <article className="case-card" key={study.title}><span>0{index + 1} · {study.industry}</span><h2>{study.title}</h2><h3>The challenge</h3><p>{study.challenge}</p><h3>The outcome</h3><p>{study.outcome}</p></article>)}</div><div className="center-cta"><h2>Working on a similar challenge?</h2><Link className="button" href="/contact">Talk to Cloudvation <span>→</span></Link></div></div></section></main><Footer /></>; }
